# === Middleware ===
asyncua==2.0.1          # servidor OPC UA (middleware/opc/server.py)
psycopg2-binary==2.9.12 # driver PostgreSQL (middleware/storage/postgres.py)
paho-mqtt==2.1.0        # cliente MQTT (usado por middleware e simulator)

# === Simulator ===
Flask==3.1.3            # dashboard web (simulator/dashboard.py)

# Observação: as demais bibliotecas usadas no projeto (json, sqlite3, queue,
# threading, asyncio, time, random, logging, abc, enum, typing) fazem parte
# da biblioteca padrão do Python e não precisam ser instaladas.
